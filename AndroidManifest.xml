<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExpanseTracker"
        android:usesCleartextTraffic="true">

        <activity android:name=".activities.SyncActivity" />
        <activity android:name=".activities.SearchActivity" />
        <activity android:name=".activities.AddEditExpenseActivity" />
        <activity android:name=".activities.ExpenseListActivity" />
        <activity android:name=".activities.ProjectDetailActivity" />
        <activity android:name=".activities.ConfirmProjectActivity" />
        <activity android:name=".activities.AddEditProjectActivity" />
        <activity android:name=".activities.ProjectListActivity" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>